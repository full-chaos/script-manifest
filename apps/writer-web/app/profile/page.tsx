export default function ProfilePage() {
  return (
    <section className="card">
      <h2>Writer Profile</h2>
      <p>Profile fields and edit flows will consume the profile-project service APIs.</p>
    </section>
  );
}
